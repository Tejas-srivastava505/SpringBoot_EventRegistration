<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!--  Spring Boot Starter Parent gives us sensible default versions
             for all Spring dependencies below, so we don't have to pick
             each version ourselves.  -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>
    <groupId>com.tejas</groupId>
    <artifactId>event-registration-api</artifactId>
    <version>1.0.0</version>
    <name>event-registration-api</name>
    <description>Event registration and check-in REST API</description>
    <properties>
        <java.version>17</java.version>
    </properties>
    <dependencies>
        <!--  Gives us @RestController, @RequestMapping, embedded Tomcat server, etc.  -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!--  Gives us JpaRepository, @Entity, @Id, etc. so we don't hand-write SQL  -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!--  Bean Validation annotations: @NotBlank, @Email, @Min, etc.  -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!--  MySQL JDBC driver, so Spring can actually talk to a MySQL database  -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!--  Generates a browser-based UI (Swagger UI) at /swagger-ui.html
                     for testing every endpoint by clicking, without needing
                     curl or Postman. Reads your @RestController classes
                     automatically — no extra config needed.  -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>